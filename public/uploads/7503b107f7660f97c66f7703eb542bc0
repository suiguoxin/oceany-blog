/*
        Dn dnObj = new Dn()
        dnObj.packages = []
        dn.json."NDL_pour_rundeck".packages.each { p ->
            dnObj.packages << p
        }
*/

//in DnController, try to turn JSONObject to DnPackage object, but it doesn't use the customised converter 
 /*  //dnPackage
          JSONObject jPackage = json.NDL_pour_rundeck.packages[0]
          //render dn as JSON
          def dnPackage = new DnPackage()
          dnPackage.properties = jPackage

          render dnPackage as JSON
 */

 //how to select format in the showDn block ? How to stock the information in the views ?
 